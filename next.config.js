/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.startech.com.bd", "icons.iconarchive.com"],
  },
};

module.exports = nextConfig;
